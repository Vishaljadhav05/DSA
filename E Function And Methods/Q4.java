public class Q4 {
   /*
   
   Question 4 : READ & CODE EXERCISE
    Search about(Google) & use the following methods of the Math class in Java:
    a. Math.min( ) :- Returns the smaller of 2 numbers.
                    => Math.min(a,b)

    b. Math.max( ) :- Retuen the larger of two number.
                    => Math.max(a,b)

    c. Math.sqrt( ) :- Returns the square root of a number.
                    => Math.sqrt(number)

    d. Math.pow( ) :- Returns the value of a number raised to powe.
                    => Math.pow(base,exponent)

    e. Math.avg( ) :- Right now does not exist in java.

    f. Math.abs( ) :- Returns the absolute (positive) value of a number.
                    => Math.abs(.number)

   */ 
}
